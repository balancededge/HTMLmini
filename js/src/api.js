function HTMLmini() {
    // Entry points
    HTMLmini.html      = HTMLmini.Compiler.html;
    HTMLmini.stringify = HTMLmini.Compiler.stringify;
    HTMLmini.compile   = HTMLmini.Compiler.compile; 
};